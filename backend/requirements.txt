# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Config & validation
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# HTTP client
httpx==0.28.1

# Scheduling
apscheduler==3.10.4

# WebSockets
websockets==14.1

# Market data
yfinance==0.2.51

# AI / LLM
openai==1.58.1

# NLP (lightweight — FinBERT/torch is opt-in via ENABLE_FINBERT=1)
transformers==4.47.1
# torch is intentionally excluded from default install to keep RAM low.
# Install separately if needed: pip install torch --index-url https://download.pytorch.org/whl/cpu

# NER
spacy==3.8.3

# Geocoding
geopy==2.4.1

# Data
pandas==2.2.3
numpy==2.2.0
requests==2.32.3
