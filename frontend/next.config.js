/** @type {import('next').NextConfig} */
const nextConfig = {
  // 'standalone' is only needed for Docker/self-hosted.
  // Vercel handles its own output format automatically.
};

module.exports = nextConfig;
